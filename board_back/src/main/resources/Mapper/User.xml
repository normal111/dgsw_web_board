<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.hs.dgsw.board_back.Domain.UserMapper">

    <select id="findAll" resultType="HashMap">
        select *
        from user
    </select>

    <select id="findById" parameterType="Long" resultType="kr.hs.dgsw.board_back.Domain.User">
        select *
        from user
        where id = #{id}
    </select>

    <insert id="add" parameterType="kr.hs.dgsw.board_back.Domain.User">
        insert into user(account,password,username,email) values(#{account}, #{password}, #{username}, #{email})
        <selectKey keyProperty="id" resultType="Long">select last_insert_id()</selectKey>
    </insert>

    <update id="modify" parameterType="kr.hs.dgsw.board_back.Domain.User">
        update user
        set account=#{account},
            password=#{password},
            username=#{username},
            email=#{email}
        where id = #{id}
    </update>

    <delete id="deleteById" parameterType="Long">
        delete
        from user
        where id = #{id}
    </delete>

</mapper>